name: note
description: An Obsidian-style note. Wikilinks must resolve.

frontmatter:
  type: object
  required: [title]
  additionalProperties: true
  properties:
    title:
      type: string
      minLength: 1

links:
  - rule: obsidian-vault
    on_ambiguous: error
    check_anchors: true
