## Team folder

This is a folder-entry note. It deliberately has no frontmatter — it's read by
tools and agents, not surfaced as a knowledge-base entry. The `folder-entry`
schema makes the frontmatter block optional while still banning leftover
scratch sections in the body.
