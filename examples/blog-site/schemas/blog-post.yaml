name: blog-post
description: A blog post under content/posts/.
entity: post

frontmatter:
  type: object
  required: [title, date, tags, author]
  additionalProperties: false
  properties:
    title:
      type: string
      minLength: 1
    date:
      type: string
      format: date
    tags:
      type: array
      items: { type: string }
      minItems: 1
    author:
      type: string
    author_profile:
      type: string
      x-entity: author
    draft:
      type: boolean

body:
  - rule: forbid-h1
  - rule: required-sections
    sections: [Summary]
  - rule: section-order
    order: [Summary, Background, Details, Conclusion]
    mode: relaxed
  - rule: forbidden-sections
    sections: [TODO, Scratch]

links:
  - rule: relative-path
    check_anchors: true
