name: post
frontmatter:
  type: object
  required: [title]
  additionalProperties: false
  properties:
    title:
      type: string

body:
  - rule: required-sections
    sections: [Summary, Conclusion]
