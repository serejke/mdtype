---
title: Internal RFC 17
rfc_number: 17
schema: schemas/rfc.yaml
---

The `schema:` field replaces the glob-matched `post` schema with `rfc`. The
`post` schema would have flagged the missing `author`; `rfc` is happy.
