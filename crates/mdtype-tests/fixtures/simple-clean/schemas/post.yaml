name: post
description: Minimal schema — title required.

frontmatter:
  type: object
  required: [title]
  additionalProperties: false
  properties:
    title:
      type: string
      minLength: 1

body: []
